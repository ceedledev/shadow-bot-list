<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../parts/head") %>
    </head>

    <body>
        <div class="header">
            <%- include("../parts/header") %>
        </div>
        <div class="main">
            <div class="container">
                <h3>File d'attente</h3>
                <subtitle>Les Bots en attente pour la vérification.</subtitle>
                <hr>
                <% if (rawBots.length == 0) { %>
                <p>Oh, il n'y a actuellement aucun bot en file d'attente pour la vérification. Votre travail est terminé.</p>
                <% } else { %>
                <div class="bot-cards">
                    <%- chunks.map(chunk => `<div class="row bot-card">${chunk.map(bot => include("../parts/botCard", {bot, queue: true, config})).join("")}</div>`).join("") %>
                </div>
                <% } %>
            </div>
        </div>
    </body>
    <footer>
        <%- include("../parts/footer") %>
        <%- include("../parts/scripts") %>
    </footer>
</html>